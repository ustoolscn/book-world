apiVersion: v1
kind: Namespace
metadata:
  name: book-world

---
apiVersion: v1
kind: Secret
metadata:
  name: book-world-secret
  namespace: book-world
type: Opaque
stringData:
  DATABASE_URL: "postgres://postgres:postgres@10.42.0.1:5432/book_world?sslmode=disable"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: book-world-config
  namespace: book-world
data:
  ADDR: ":8080"
  STATIC_DIR: "/app/frontend"
  DEFAULT_MODEL: "gpt-4o-mini"
  CONTEXT_CHAR_BUDGET: "48000"
  REPLY_CHAR_RESERVE: "6000"
  FRONTEND_ORIGIN: "http://book-world.cooper-api.com"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-world-deployment
  namespace: book-world
spec:
  replicas: 1
  selector:
    matchLabels:
      app: book-world
  template:
    metadata:
      labels:
        app: book-world
    spec:
      containers:
        - name: book-world
          image: ghcr.io/ustoolscn/book-world:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: book-world-config
            - secretRef:
                name: book-world-secret

---
apiVersion: v1
kind: Service
metadata:
  name: book-world-service
  namespace: book-world
spec:
  selector:
    app: book-world
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: book-world-ingress
  namespace: book-world
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: book-world.cooper-api.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: book-world-service
                port:
                  number: 8080
